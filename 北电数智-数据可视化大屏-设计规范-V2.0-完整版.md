# 通智算集群监控中心 - 设计规范

## 文档信息

| 项目     | 内容                                                                                   |
| -------- | -------------------------------------------------------------------------------------- |
| 文档名称 | 北电数智dashboard · 设计规范                                                           |
| 当前版本 | **V2.0**                                                                               |
| 最近更新 | 2026 年 4 月 3 日                                                                      |
| 适用场景 | 集群监控、算力运营、推理/训练看板类页面                                                |
| 使用方式 | 按 §2–§13 的 Token、组件与自检项在任意技术栈实现即可，**不依赖**特定示例仓库或文件名。 |

## 目录

| 章     | 标题                                         |
| ------ | -------------------------------------------- |
| §1     | [设计概述](#1-设计概述)                      |
| §2     | [颜色系统](#2-颜色系统)                      |
| §3     | [字体系统](#3-字体系统)                      |
| §4     | [间距系统](#4-间距系统)                      |
| §5     | [组件规范](#5-组件规范)                      |
| §6     | [图表规范](#6-图表规范)                      |
| §7     | [动画规范](#7-动画规范)                      |
| §8     | [布局规范](#8-布局规范)                      |
| §9     | [Logo 规范](#9-logo-规范)                    |
| §10    | [CSS 变量完整清单](#10-css-变量完整清单)     |
| §11    | [设计原则](#11-设计原则)                     |
| §12    | [移动端适配标准](#12-移动端适配标准)         |
| §13    | [交付自检约束（强制）](#13-交付自检约束强制) |
| 附录 A | [修订历史（简表）](#附录-a-修订历史简表)     |

---

## 1. 设计概述

### 1.1 设计理念

- **科技感**：采用蓝色系为主色调，营造专业、科技的氛围
- **层次分明**：通过渐变、阴影、透明度营造视觉层次
- **双主题**：支持浅色/深色模式切换，适应不同使用场景
- **响应式**：适配不同屏幕尺寸，保证良好的用户体验

### 1.2 整体风格

- 圆角设计：统一使用圆角，柔和视觉感受
- 渐变应用：页面背景、按钮、芯片、图表折线/面积等**仅使用本文件列出的 Token**；不额外叠加未文档化的全屏光晕、面板内光、指标卡底条等氛围层
- 微动效：刷新旋转、主题切换、图表入场等**按需启用**；实现工程可省略卡片入场/悬停位移以保持克制

### 1.3 实现边界（与 V1.5+ 代码对齐）

以下条目用于约定**允许出现的效果与禁止叠加的装饰**，落地时应统一为「规范内可见、规范外不加」，避免各端各自加层导致漂移：

| 类别                  | 采用                                  | 不采用（避免与规范漂移）                               |
| --------------------- | ------------------------------------- | ------------------------------------------------------ |
| 页面背景              | `body` 三色线性渐变（`--page-bg-*`）  | `body::before` / `::after` 装饰层、独立「page-glow」层 |
| 面板 `.panel`         | 玻璃感表面 + `--shadow-panel`         | 面板顶线高光、内发光伪元素、额外描边动画               |
| 指标卡 `.metric-card` | `--metric-shadow`、`--subcard-border` | 底部彩色渐变条、未文档化的 `--metric-glow`             |
| 图表 `.chart-shell`   | 点阵背景（`--chart-grid`）            | 横向细网格线、hatch 竖线纹理、折线 SVG glow 滤镜       |
| 仪表盘 / Gauge        | 描边与轨道色                          | 整块 `drop-shadow` 外发光                              |
| 演示区块              | 业务卡片与图表                        | 独立「色板演示卡」「趋势说明行」等非产品模块           |

静态 HTML 原型若仍保留卡片 `fadeInUp` 或 `:hover` 上浮，需在交付说明中标注为**可选动效**；与 replica 工程不一致时以本表为准。

---

## 2. 颜色系统

### 2.1 语义色

| 颜色名称      | 色值      | 用途               |
| ------------- | --------- | ------------------ |
| Success       | `#4be38e` | 成功状态、正向指标 |
| Warning       | `#ffbf5b` | 警告状态、提醒信息 |
| Danger        | `#ff7a82` | 错误状态、危险提示 |
| Accent Cyan   | `#60d8ff` | 强调色-青色        |
| Accent Teal   | `#5fd7bc` | 强调色-青绿        |
| Accent Yellow | `#ffd06a` | 强调色-黄色        |

### 2.2 蓝色色阶（Blue Scale）

用于进度条、图表、Token 使用量横向条形图等场景，提供从深到浅的5级蓝色：

| Token      | 色值      | 用途                          | 示例                     |
| ---------- | --------- | ----------------------------- | ------------------------ |
| `--blue-1` | `#4d7cff` | 最深蓝 - 主要进度条、图表主色 | 进度条填充、折线图主线条 |
| `--blue-2` | `#5a8dff` | 深蓝 - 次要进度、图表辅助色   | 双色条形图第二段         |
| `--blue-3` | `#6fa8ff` | 中蓝 - 中间过渡色             | 多段条形图中间段         |
| `--blue-4` | `#93c5fd` | 浅蓝 - 辅助数据展示           | Token 输出段、次要数据   |
| `--blue-5` | `#b8d4fe` | 最浅蓝 - 背景装饰、占位       | 条形图末段、背景装饰     |

**应用场景：**

1. **进度条统一颜色**：所有进度条统一使用 `--blue-1` (#4d7cff)

   ```css
   .progress-fill {
     background: linear-gradient(90deg, var(--blue-1) 0%, #6fa8ff 100%);
   }
   ```

2. **Token 使用量横向条形图**：使用5级蓝色展示分段数据

   ```html
   <div class="h-12 rounded overflow-hidden flex">
     <div style="width: 20%; background: var(--blue-1);"></div>
     <div style="width: 20%; background: var(--blue-2);"></div>
     <div style="width: 20%; background: var(--blue-3);"></div>
     <div style="width: 20%; background: var(--blue-4);"></div>
     <div style="width: 20%; background: var(--blue-5);"></div>
   </div>
   ```

3. **图表多序列**：折线图、柱状图的多组数据可按顺序使用蓝色色阶
   ```javascript
   datasets: [
       { borderColor: 'var(--blue-1)', ... },
       { borderColor: 'var(--blue-4)', ... }
   ]
   ```

**固定约束：**

- ✅ 所有进度条必须使用 `--blue-1` (#4d7cff) 作为主色
- ✅ 横向条形图分段必须使用5级蓝色色阶
- ✅ 不得使用蓝色色阶以外的颜色作为进度条填充色
- ✅ 蓝色色阶的色值不得随意修改

### 2.3 全局 Token（`:root`，双主题共用）

| Token                                    | 典型值               | 用途                                                             |
| ---------------------------------------- | -------------------- | ---------------------------------------------------------------- |
| `--font-ui`                              | 见 §3.1              | 界面正文字体栈（系统 UI + 中文回退）                             |
| `--font-data-display`                    | 见 §3.1              | 数据展示 / 英文数字混排（可选）                                  |
| `--font-mono`                            | 见 §3.1              | 时间、代码、等宽数据                                             |
| `--nav-pill-active-bg`                   | `#2775ff`            | 导航胶囊选中背景                                                 |
| `--nav-pill-active-text`                 | `#ffffff`            | 导航胶囊选中文字                                                 |
| `--chart-series-1` … `--chart-series-10` | 见 §2.6 / 主题内覆盖 | 多序列图表默认色（浅色与深色下数值可与 §2.6 表一致或按主题微调） |

### 2.4 浅色模式 (Light Theme)

#### 文字颜色

| Token              | 色值      | 用途             |
| ------------------ | --------- | ---------------- |
| `--text-primary`   | `#16233a` | 主要文字、标题   |
| `--text-secondary` | `#4d6587` | 次要文字、描述   |
| `--text-muted`     | `#7d90ac` | 辅助文字、占位符 |
| `--accent`         | `#2f6df6` | 强调色、链接     |

#### 背景颜色

| Token             | 色值                        | 用途           |
| ----------------- | --------------------------- | -------------- |
| `--page-bg-start` | `#edf4ff`                   | 页面背景起始色 |
| `--page-bg-mid`   | `#f7fbff`                   | 页面背景中间色 |
| `--page-bg-end`   | `#e7f0ff`                   | 页面背景结束色 |
| `--panel-surface` | `rgba(255, 255, 255, 0.98)` | 面板背景       |
| `--bg-card`       | `rgba(255, 255, 255, 0.98)` | 卡片背景       |
| `--bg-card-soft`  | `rgba(248, 251, 255, 0.98)` | 柔和卡片背景   |

#### 边框颜色

| Token              | 色值                        | 用途       |
| ------------------ | --------------------------- | ---------- |
| `--line`           | `rgba(126, 155, 208, 0.24)` | 普通边框   |
| `--line-strong`    | `rgba(84, 126, 228, 0.28)`  | 强调边框   |
| `--button-border`  | `rgba(123, 154, 211, 0.22)` | 按钮边框   |
| `--subcard-border` | `rgba(126, 155, 208, 0.16)` | 子卡片边框 |

#### 阴影

| Token             | 色值                                                                           | 用途         |
| ----------------- | ------------------------------------------------------------------------------ | ------------ |
| `--shadow-panel`  | `0 24px 56px rgba(40, 72, 120, 0.14), inset 0 1px 0 rgba(255, 255, 255, 0.8)`  | 面板阴影     |
| `--metric-shadow` | `0 16px 32px rgba(59, 90, 138, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.78)` | 指标卡片阴影 |

### 2.5 深色模式 (Dark Theme)

#### 文字颜色

| Token              | 色值      | 用途             |
| ------------------ | --------- | ---------------- |
| `--text-primary`   | `#f3f7ff` | 主要文字、标题   |
| `--text-secondary` | `#c5d3ef` | 次要文字、描述   |
| `--text-muted`     | `#7386af` | 辅助文字、占位符 |
| `--accent`         | `#79aaff` | 强调色、链接     |

#### 背景颜色

| Token             | 色值                  | 用途           |
| ----------------- | --------------------- | -------------- |
| `--page-bg-start` | `#0a1530`             | 页面背景起始色 |
| `--page-bg-mid`   | `#050b15`             | 页面背景中间色 |
| `--page-bg-end`   | `#040912`             | 页面背景结束色 |
| `--panel-surface` | `rgba(18, 35, 65, 1)` | 面板背景       |
| `--bg-card`       | `rgba(22, 42, 78, 1)` | 卡片背景       |
| `--bg-card-soft`  | `rgba(20, 38, 72, 1)` | 柔和卡片背景   |

#### 边框颜色

| Token              | 色值                        | 用途                         |
| ------------------ | --------------------------- | ---------------------------- |
| `--line`           | `rgba(100, 147, 255, 0.18)` | 普通边框                     |
| `--line-strong`    | `rgba(121, 169, 255, 0.34)` | 强调边框                     |
| `--button-border`  | `rgba(112, 149, 235, 0.16)` | 按钮边框                     |
| `--subcard-border` | `rgba(112, 148, 233, 0.12)` | 子卡片边框（replica 当前值） |

#### 阴影

| Token             | 色值                                                                       | 用途                           |
| ----------------- | -------------------------------------------------------------------------- | ------------------------------ |
| `--shadow-panel`  | `0 24px 60px rgba(0, 0, 0, 0.34), inset 0 1px 0 rgba(255, 255, 255, 0.06)` | 面板阴影                       |
| `--metric-shadow` | `0 18px 36px rgba(0, 0, 0, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.04)`  | 指标卡片阴影（replica 当前值） |

### 2.6 图表色板

| 序号 | 浅色模式  | 深色模式  | 用途      |
| ---- | --------- | --------- | --------- |
| 1    | `#B7CAFF` | `#79aaff` | 主色-蓝   |
| 2    | `#A0E6CF` | `#5fd7bc` | 辅色-青绿 |
| 3    | `#C7EECC` | `#60d8ff` | 辅色-青   |
| 4    | `#FFEA94` | `#ffd06a` | 辅色-黄   |
| 5    | `#CBBABA` | `#d2a8ff` | 辅色-紫   |
| 6    | `#E8B7BD` | `#ff7a82` | 辅色-红   |
| 7    | `#B9BEE0` | `#8d96ff` | 辅色-蓝紫 |
| 8    | `#B4CCB9` | `#7ce7a2` | 辅色-绿   |
| 9    | `#E8D3B7` | `#ff9f5f` | 辅色-橙   |
| 10   | `#D3BFD1` | `#ff8ec7` | 辅色-粉   |

**浅色模式色板说明：**

- 浅色模式使用**低饱和度/静音化**颜色，视觉更柔和
- 采用莫兰迪色系，降低认知负荷，增强视觉舒适度
- 与深色模式形成对比，深色模式颜色更鲜艳

---

## 3. 字体系统

### 3.1 字体家族

```css
--font-ui:
  -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "Noto Sans SC",
  "Source Han Sans SC", Arial, sans-serif;

--font-data-display: "Roboto", Arial, sans-serif;

--font-mono: "SF Mono", "Menlo", "Monaco", "Roboto Mono", "Inconsolata", ui-monospace, monospace;
```

### 3.2 字号规范

| 名称     | 字号            | 字重           | 用途       |
| -------- | --------------- | -------------- | ---------- |
| 标题 H1  | 24px (text-2xl) | 600 (semibold) | 页面主标题 |
| 标题 H2  | 18px (text-lg)  | 600 (semibold) | 卡片标题   |
| 正文     | 14px (text-sm)  | 400 (normal)   | 常规内容   |
| 辅助文字 | 12px (text-xs)  | 400 (normal)   | 标签、说明 |
| 大数字   | 30px (text-3xl) | 600 (semibold) | 指标数值   |

### 3.3 数值与单位间距

数值与单位之间统一使用 **4px** 间距，通过 `ml-1` 类实现：

```html
<!-- 正确示例 -->
<div class="text-2xl font-bold text-primary">
  453.8<span class="text-sm font-normal text-muted ml-1">元</span>
</div>

<!-- 其他示例 -->
<span class="text-sm font-normal text-muted ml-1">°C</span>
<span class="text-sm font-normal text-muted ml-1">W</span>
<span class="text-sm font-normal text-muted ml-1">kW</span>
<span class="text-sm font-normal text-muted ml-1">req/s</span>
<span class="text-sm font-normal text-muted ml-1">/s</span>
<span class="text-sm font-normal text-muted ml-1">张</span>
<span class="text-sm font-normal text-muted ml-1">s</span>
```

| 单位类型 | 示例       |
| -------- | ---------- |
| 温度     | °C         |
| 功率     | W, kW      |
| 货币     | 元, 元/kWh |
| 速率     | req/s, /s  |
| 数量     | 张         |
| 时间     | s, ms      |

### 3.4 等宽字体

用于时间显示、代码等场景；**须使用** `var(--font-mono)`，与 §3.1、§10.1 中 `--font-mono` 定义保持一致：

```css
font-family: var(--font-mono);
```

---

## 4. 间距系统

### 4.1 基础间距

基于 4px 网格系统：

| Token | Tailwind Class | 像素值 | 用途     |
| ----- | -------------- | ------ | -------- |
| xs    | p-1, gap-1     | 4px    | 最小间距 |
| sm    | p-2, gap-2     | 8px    | 小间距   |
| md    | p-3, gap-3     | 12px   | 中等间距 |
| base  | p-4, gap-4     | 16px   | 基础间距 |
| lg    | p-6, gap-6     | 24px   | 大间距   |
| xl    | p-8, gap-8     | 32px   | 超大间距 |

### 4.2 组件间距

| 组件       | 内边距                     | 外边距       |
| ---------- | -------------------------- | ------------ |
| 页面容器   | p-6 (24px)                 | -            |
| 卡片       | p-6 (24px)                 | mb-4 (16px)  |
| 按钮(圆形) | p-10 (40px直径)            | gap-3 (12px) |
| 状态标签   | 4px 10px                   | -            |
| 导航胶囊   | 4px (内) / 6px 10px (项目) | -            |

### 4.3 卡片间距

- 卡片之间间距：16px (gap-4)
- 卡片区域之间间距：16px (mb-4)

### 4.3.1 栅格占位约束（禁止半行空白）

- 在任何断点下，同一行内卡片的 `col-span` 之和必须**填满该行的列数**，不得出现明显的「半行空白」。
- 典型组合示例（以 Tailwind 为例，仅示意）：
  - 一行 1 张卡：`grid-cols-1` 或 `lg:col-span-3`（当 `lg:grid-cols-3` 时）。
  - 一行 2 张卡：`lg:grid-cols-2`，两张卡均为 `lg:col-span-1`。
  - 一行 3 张卡：`lg:grid-cols-3`，三张卡均为 `lg:col-span-1`，或 `lg:col-span-2 + lg:col-span-1` 但总和 = 3。
- **禁止示例**：`lg:grid-cols-3` 但该行只有一张 `panel`，并且仍是 `lg:col-span-1`，右侧留下两个空栅格（视觉上是一大块空白）。
- **适用范围**：除了已在 §4.6.1 / §4.6.2 中明确要求「独占一行」的特殊卡片外，其余卡片（如「身体指标快照」「健康提醒」等）在编排时均应遵守本约束。

### 4.4 卡片内部间距

| 元素         | 间距        | 说明                                             |
| ------------ | ----------- | ------------------------------------------------ |
| 标题与内容   | 16px (mb-4) | 卡片标题(h2)与下方内容的间距                     |
| 内容区块之间 | 16px (mb-4) | 同一卡片内不同内容区块的间距（如数值与图表之间） |
| 数据与表格   | 16px (mb-4) | 上半部分数据与下半部分表格的间距                 |

```css
/* 卡片标题间距规范 */
.card h2 {
  margin-bottom: 16px; /* mb-4 */
}

/* 内容区块间距 */
.card .content-block {
  margin-bottom: 16px; /* mb-4 */
}

/* 数据区块间距 */
.card .data-section {
  margin-bottom: 16px; /* mb-4 */
}
```

### 4.5 卡片内容与底部间距

卡片内容与卡片底部的间距统一使用 **24px (p-6)**，通过卡片内边距实现：

```css
.panel {
  padding: 24px; /* p-6 */
}
```

**间距说明：**

- 卡片内边距：24px（上下左右）
- 内容自动与卡片边缘保持 24px 间距
- 使用 `flex flex-col` 布局时，最后一个元素自动与底部保持 24px 间距

### 4.6 特殊卡片类型

#### 4.6.1 总分数据卡片

用于展示"总量 + 子项分布"的数据结构，如 Token 使用量。

**组件级约束：**

- 卡片内部结构必须包含四个连续区块：**标题 + 时间范围按钮** → **总量数据（大数字 + 单位）** → **分段条形图（总量按子项拆分）** → **标注线 + 数值标签**，中间纵向间距统一为 16px（mb-4）。
- 分段条形图仅允许使用 **§2.2 蓝色色阶（`--blue-1` … `--blue-5`）**，不得引入额外色系；主段优先使用 `--blue-1`，其余子段按深→浅依次使用。
- 每一段对应的**虚线标注**必须对齐该段的**左侧起点**（而不是整条总进度条的右端），以便清晰表达「该分段自此开始」。
- 标注文案推荐格式：`子项标签 · 数值 + 单位`（如「训练 · 53 分」），避免出现「53 分 训练」这类易产生歧义或在小屏被截断的组合。

**布局结构：**

```
┌─────────────────────────────────┐
│ 卡片标题           时间范围按钮  │
│ ↓ 16px                          │
├─────────────────────────────────┤
│ 总量数据（大数字 + 单位）        │
│ ↓ 16px                          │
│ 分段条形图（总 → 子项分布）      │
│ ↓ 16px                          │
│ 标注线 + 数值标签               │
└─────────────────────────────────┘
```

**示例：Token 使用量**

```html
<div class="panel rounded-2xl p-6">
  <div class="flex items-center justify-between mb-4">
    <h2 class="text-lg font-semibold text-primary">Token 使用量</h2>
    <div class="flex gap-px">
      <button class="time-btn active">1天</button>
      <button class="time-btn">7天</button>
      <button class="time-btn">30天</button>
    </div>
  </div>

  <!-- 总量数据 -->
  <div class="mb-4">
    <span class="text-3xl font-bold text-primary">1,234,567</span>
    <span class="text-sm text-muted ml-1">个</span>
  </div>

  <!-- 分段条形图 -->
  <div class="mb-4">
    <div class="h-6 rounded-lg overflow-hidden flex">
      <div class="bg-blue-500" style="width: 70%;"></div>
      <div class="bg-blue-300" style="width: 30%;"></div>
    </div>
  </div>

  <!-- 标注线 + 数值标签 -->
  <div class="relative">
    <div class="absolute border-l-2 border-dashed border-gray-400" style="left: 70%; height: 24px;">
      <span class="text-xs text-secondary ml-2">856,234个 输入</span>
    </div>
    <div
      class="absolute border-l-2 border-dashed border-gray-400"
      style="left: 100%; height: 24px;"
    >
      <span class="text-xs text-secondary ml-2">378,333个 输出</span>
    </div>
  </div>
</div>
```

**适用场景：**

- Token 使用量：总 Token → 输入 Token + 输出 Token
- GPU 分配：总 GPU → 各租户分配
- 资源使用：总资源 → 各类型使用

#### 4.6.2 数据 + 表格卡片

用于展示"关键指标 + 详细列表"的数据结构。

**新增约束：独占一行**

- 数据 + 表格卡片必须在外层布局中**独占一行**：同一 grid row 内不得与其他 `panel` 并列。
- 实现建议：在大屏断点（如 `lg` 及以上）将该卡片设为 `col-span` 满列（例如 `lg:col-span-3` 或等价），从而保证视觉边界清晰、表格可读性稳定。

**布局结构：**

```
┌─────────────────────────────────┐
│ 卡片标题                        │
│ ↓ 16px                          │
├─────────────────────────────────┤
│ 关键指标区域（多个数据项）       │
│ ↓ 16px                          │
├─────────────────────────────────┤
│ 表格区域                        │
└─────────────────────────────────┘
```

**示例：模型推理监控**

```html
<div class="panel rounded-2xl p-6">
  <h2 class="text-lg font-semibold text-primary mb-4">模型推理监控</h2>

  <!-- 关键指标 -->
  <div class="grid grid-cols-3 gap-4 mb-4">
    <div>
      <div class="text-muted text-sm">总 QPS</div>
      <div class="text-2xl font-bold text-primary">
        0.32<span class="text-sm text-muted ml-1">req/s</span>
      </div>
    </div>
    <div>
      <div class="text-muted text-sm">总 Input Token 速率</div>
      <div class="text-2xl font-bold text-primary">
        5986.5<span class="text-sm text-muted ml-1">/s</span>
      </div>
    </div>
    <div>
      <div class="text-muted text-sm">总 Output Token 速率</div>
      <div class="text-2xl font-bold text-primary">
        220.1<span class="text-sm text-muted ml-1">/s</span>
      </div>
    </div>
  </div>

  <!-- 表格 -->
  <div class="overflow-x-auto">
    <table class="w-full text-sm">
      <!-- 表格内容 -->
    </table>
  </div>
</div>
```

**适用场景：**

- 模型推理监控
- GPU 健康看板
- Volcano 任务队列
- Pod 异常监控

### 4.7 无数据卡片排序规则

当页面中存在无数据卡片时，应将无数据卡片放置在有数据卡片之后。

**排序原则：**

1. 有数据的卡片优先显示
2. 无数据或空状态的卡片放在后面
3. 同类卡片保持原有顺序

**示例布局：**

```
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ 有数据卡片1 │ │ 有数据卡片2 │ │ 有数据卡片3 │ │ 无数据卡片  │
└─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘
```

**无数据卡片样式：**

```html
<div class="panel rounded-2xl p-6 opacity-60">
  <h2 class="text-lg font-semibold text-primary mb-4">卡片标题</h2>
  <div class="flex items-center justify-center h-32 text-muted">暂无数据</div>
</div>
```

### 4.8 时间范围按钮间距

| 元素     | 间距         | 说明                       |
| -------- | ------------ | -------------------------- |
| 按钮之间 | 1px (gap-px) | 时间范围选择按钮之间的间距 |

```css
/* 时间按钮容器 */
.time-btn-group {
  display: flex;
  gap: 1px; /* gap-px */
}

/* 时间按钮样式 */
.time-btn {
  padding: 4px 10px;
  font-size: 12px;
  font-weight: 500;
  color: var(--text-muted);
  background: transparent;
  border: none;
  cursor: pointer;
}

.time-btn:hover {
  color: var(--text-primary);
}

.time-btn.active {
  color: #2775ff;
  font-weight: 600;
}
```

**适用场景：**

- GPU 利用率趋势：30m、1h、6h、24h
- Token 使用量：1天、7天、30天
- 节点资源 Top10：CPU、内存、磁盘、网络

### 4.9 卡片底部对齐规范

同一行内的卡片，内容底部应对齐，使用 flex 布局实现：

```html
<!-- 卡片底部对齐结构 -->
<div class="panel rounded-2xl p-6 flex flex-col">
  <h2 class="text-lg font-semibold text-primary mb-4">卡片标题</h2>
  <div class="flex-1 flex flex-col justify-end">
    <!-- 内容区域 -->
  </div>
</div>
```

**实现原理：**

- 卡片容器：`flex flex-col` - 使卡片内容垂直排列
- 内容容器：`flex-1 flex flex-col justify-end` - 占据剩余空间，内容底部对齐

**适用场景：**

- 同一行内多个卡片高度不一致时
- 需要卡片内容底部对齐时

### 4.10 条形图渐变规范

条形图使用蓝色色阶，统一使用 `--blue-1` 作为主色：

```css
.progress-fill {
  height: 100%;
  border-radius: 3px;
  background: linear-gradient(90deg, var(--blue-1) 0%, var(--blue-3) 100%);
}
```

**颜色说明：**

- 起始色：`var(--blue-1)` (#4d7cff) - 主蓝色
- 结束色：`var(--blue-3)` (#6fa8ff) - 中蓝色
- 渐变方向：从左到右 (90deg)

**固定约束：**

- ✅ 所有进度条必须使用蓝色色阶
- ✅ 主色使用 `--blue-1` (#4d7cff)
- ✅ 详见 §2.2 蓝色色阶

### 4.11 排行榜数值颜色规范

排行榜列表中的数值使用次要文字颜色（灰色），不使用强调色，与表格中的灰色保持一致：

```css
/* 排行榜数值样式 */
.rank-value {
  color: var(--text-secondary); /* text-secondary */
}
```

**适用场景：**

- 按服务 Token 分布：数值如 92,093,021
- 节点资源 Top10：百分比如 92%
- Notebook 开发环境：数量信息如 "32个 · 11运行中"

**颜色一致性：**

- 排行榜数值颜色 = 表格中的次要文字颜色 (`text-secondary`)
- 浅色模式：`#4d6587`
- 深色模式：`#c5d3ef`

### 4.12 图表容器规范

| 元素           | 尺寸        | 说明                     |
| -------------- | ----------- | ------------------------ |
| 图表容器高度   | 176px       | 统一的图表容器高度       |
| 标题与图表间距 | 16px (mb-4) | 卡片标题与图表容器的间距 |

```css
/* 图表容器样式 */
.chart-shell {
  height: 176px;
  padding: 16px;
  border-radius: 12px;
}

/* 卡片标题间距 */
.card-title {
  margin-bottom: 16px; /* mb-4 */
}
```

**布局结构：**

```
┌─────────────────────────────────┐
│ 卡片标题 (h2)                    │
│ ↓ 16px 间距 (mb-4)               │
├─────────────────────────────────┤
│                                 │
│ 图表容器 (176px)                 │
│                                 │
└─────────────────────────────────┘
```

---

## 5. 组件规范

### 5.1 面板 (Panel)

```css
.panel {
  background: var(--panel-surface);
  box-shadow: var(--shadow-panel);
  border-radius: 16px;
  overflow: hidden;
  position: relative;
  z-index: 1;
}
```

**阴影遮挡约束：**

- ✅ 所有面板必须设置 `position: relative` 和 `z-index: 1`
- ✅ 确保卡片不被其他卡片的阴影遮挡
- ✅ 阴影应在卡片下方显示，不得覆盖相邻卡片内容
- ✅ 使用标准 z-index 值，避免层级混乱

### 5.2 卡片 (Card)

```css
.card {
  background: var(--bg-card);
  border: 1px solid var(--subcard-border);
  box-shadow: var(--subcard-shadow);
  border-radius: 16px;
  position: relative;
  z-index: 1;
}
```

**阴影遮挡约束：**

- ✅ 所有卡片必须设置 `position: relative` 和 `z-index: 1`
- ✅ 确保卡片不被其他卡片的阴影遮挡
- ✅ 阴影应在卡片下方显示，不得覆盖相邻卡片内容
- ✅ 与面板保持相同的 z-index 值，避免层级混乱

### 5.3 按钮 (Button)

#### 圆形按钮

```css
.theme-toggle,
.link-pill {
  padding: 10px;
  border-radius: 50%;
  background: var(--button-bg);
  border: 1px solid var(--button-border);
  width: 40px;
  height: 40px;
}
```

#### 刷新按钮

```css
.refresh-btn {
  padding: 4px;
  border-radius: 50%;
  background: transparent;
  border: 1px solid var(--button-border);
}
```

### 5.4 状态标签 (Status Chip)

| 状态        | 背景色                      | 文字色    |
| ----------- | --------------------------- | --------- |
| 运行中/成功 | `rgba(75, 227, 142, 0.14)`  | `#4be38e` |
| 可用        | `rgba(77, 124, 255, 0.14)`  | `#4d7cff` |
| 活跃        | `rgba(251, 146, 60, 0.14)`  | `#fb923c` |
| 警告        | `rgba(255, 191, 91, 0.14)`  | `#ffbf5b` |
| 错误        | `rgba(255, 122, 130, 0.14)` | `#ff7a82` |

```css
.status-chip {
  padding: 4px 10px;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 500;
}
```

### 5.5 导航胶囊 (Nav Pill)

选中态背景与文字**优先使用 Token**，与主题切换、时间范围等胶囊一致：

```css
.nav-pill-container {
  display: flex;
  align-items: center;
  gap: 1px;
  padding: 4px;
  height: 40px;
  background: var(--panel-surface);
  border: 1px solid var(--line);
  border-radius: 9999px;
}

.nav-pill-item {
  padding: 6px 10px;
  border-radius: 9999px;
  font-size: 12px;
  font-weight: 500;
}

.nav-pill-item.is-active,
.nav-pill-item.active {
  background: var(--nav-pill-active-bg, #2775ff);
  color: var(--nav-pill-active-text, #fff);
}
```

### 5.6 页面顶部结构 (Header)

页面顶部结构包含四个主要部分，需严格按照以下规范实现：

#### 5.6.1 整体结构

```
┌─────────────────────────────────────────────────────────────┐
│ header (flex justify-between items-center mb-2)            │
│ ┌──────────────────────┐  ┌──────────────────────────────┐ │
│ │ Logo + 标题          │  │ 时间 + 主题 + 下载 + 设置    │ │
│ └──────────────────────┘  └──────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────────┐
│ 导航胶囊 + 更新时间/刷新 (flex items-center justify-between)│
│ ┌──────────────────────┐  ┌──────────────────────────────┐ │
│ │ 导航胶囊容器         │  │ 更新时间 + 刷新按钮          │ │
│ └──────────────────────┘  └──────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 5.6.2 Header 元素规范

**HTML 结构：**

```html
<header class="flex justify-between items-center mb-2">
  <!-- 左侧：Logo + 标题 -->
  <div class="flex items-center gap-3">
    <div class="logo-icon">
      <svg class="w-10 h-10" viewBox="0 0 40 40">...</svg>
    </div>
    <h1 class="text-2xl font-semibold text-primary">通智算集群监控中心</h1>
  </div>

  <!-- 右侧：时间 + 操作按钮 -->
  <div class="flex gap-1 items-center">
    <div class="time-display-vertical" id="timeDisplay">
      <span id="currentDate">2026-04-02</span>
      <span id="currentTime">09:38:13</span>
    </div>
    <button class="theme-toggle" onclick="toggleTheme()">...</button>
    <a href="#" class="link-pill" title="导出报告">...</a>
    <a href="#" class="link-pill" title="设置">...</a>
  </div>
</header>
```

**CSS 规范：**

```css
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8px; /* mb-2 */
}

/* Logo 图标 */
.logo-icon {
  width: 40px;
  height: 40px;
}

.logo-icon svg {
  width: 100%;
  height: 100%;
}

/* 页面标题 */
header h1 {
  font-size: 24px; /* text-2xl */
  font-weight: 600; /* font-semibold */
  color: var(--text-primary);
}

/* 右侧操作区域 */
header .flex.gap-1.items-center {
  display: flex;
  gap: 4px; /* gap-1 */
  align-items: center;
}
```

#### 5.6.3 导航胶囊容器规范

**HTML 结构：**

```html
<div class="flex items-center justify-between mb-4">
  <!-- 左侧：导航胶囊 -->
  <div class="nav-pill-container">
    <a href="#" class="nav-pill-item active">
      <svg class="w-3.5 h-3.5">...</svg>
      <span>利用率热力图</span>
    </a>
    <a href="#" class="nav-pill-item">
      <svg class="w-3.5 h-3.5">...</svg>
      <span>单卡明细</span>
    </a>
    <a href="#" class="nav-pill-item">
      <svg class="w-3.5 h-3.5">...</svg>
      <span>租户ROI</span>
    </a>
  </div>

  <!-- 右侧：更新时间 + 刷新 -->
  <div class="flex items-center gap-2">
    <span class="text-secondary text-sm">更新时间：<span id="updateTime">09:38:19</span></span>
    <button class="refresh-btn" onclick="refreshData()">...</button>
  </div>
</div>
```

**CSS 规范：**

```css
/* 导航胶囊 + 更新时间容器 */
.flex.items-center.justify-between.mb-4 {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px; /* mb-4 */
}

/* 导航胶囊容器 */
.nav-pill-container {
  display: flex;
  align-items: center;
  gap: 1px;
  padding: 4px;
  height: 40px;
  background: var(--panel-surface);
  border: 1px solid var(--line);
  border-radius: 9999px;
}

/* 导航胶囊项 */
.nav-pill-item {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  border-radius: 9999px;
  font-size: 12px;
  font-weight: 500;
  color: var(--text-muted);
  text-decoration: none;
  transition: all 0.2s ease;
}

.nav-pill-item:hover {
  color: var(--text-primary);
  background: rgba(77, 124, 255, 0.08);
}

.nav-pill-item.active {
  background: var(--nav-pill-active-bg, #2775ff);
  color: var(--nav-pill-active-text, #fff);
}

/* 导航胶囊图标 */
.nav-pill-item svg {
  width: 14px; /* w-3.5 */
  height: 14px; /* h-3.5 */
}

/* 更新时间 + 刷新容器 */
.flex.items-center.gap-2 {
  display: flex;
  align-items: center;
  gap: 8px; /* gap-2 */
}
```

#### 5.6.4 时间显示规范

**纵向布局（用于 header）：**

```css
.time-display-vertical {
  display: flex;
  flex-direction: column;
  gap: 4px;
  font-family: var(--font-mono);
}

.time-display-vertical span:first-child {
  font-size: 11px;
  color: var(--text-muted);
  opacity: 0.8;
}

.time-display-vertical span:last-child {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-primary);
}
```

#### 5.6.5 操作按钮规范

**圆形按钮（主题切换、下载、设置）：**

```css
.theme-toggle,
.link-pill {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  padding: 10px;
  border-radius: 50%;
  background: var(--button-bg);
  border: 1px solid var(--button-border);
  color: var(--text-primary);
  cursor: pointer;
  transition: all 0.2s ease;
}

.theme-toggle:hover,
.link-pill:hover {
  background: var(--button-bg-hover);
  transform: scale(1.05);
}

.theme-toggle svg,
.link-pill svg {
  width: 20px; /* w-5 */
  height: 20px; /* h-5 */
}
```

**刷新按钮：**

```css
.refresh-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  padding: 8px;
  border-radius: 50%;
  background: transparent;
  border: 1px solid var(--button-border);
  color: var(--text-muted);
  cursor: pointer;
  transition: all 0.2s ease;
}

.refresh-btn:hover {
  background: var(--button-bg);
  color: var(--text-primary);
}

.refresh-btn svg {
  width: 16px; /* w-4 */
  height: 16px; /* h-4 */
}

/* 刷新动画 */
.refresh-btn.refreshing svg {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
```

#### 5.6.6 间距规范

| 元素               | 间距值 | Tailwind 类 | 说明                         |
| ------------------ | ------ | ----------- | ---------------------------- |
| header 底部        | 8px    | `mb-2`      | header 与下方内容的间距      |
| Logo 与标题        | 12px   | `gap-3`     | Logo 图标与标题文字的间距    |
| 时间与按钮组       | 4px    | `gap-1`     | 时间显示与操作按钮的间距     |
| 操作按钮之间       | 4px    | `gap-1`     | 主题、下载、设置按钮的间距   |
| 导航胶囊容器底部   | 16px   | `mb-4`      | 导航胶囊与下方内容的间距     |
| 更新时间与刷新按钮 | 8px    | `gap-2`     | 更新时间文字与刷新按钮的间距 |
| 导航胶囊项之间     | 1px    | `gap-1px`   | 各导航项之间的间距           |

#### 5.6.7 响应式适配

**Mobile (< 768px)：**

```css
/* header 在移动端调整 */
@media (max-width: 767px) {
  header {
    flex-direction: column;
    align-items: flex-start;
    gap: 12px;
  }

  header h1 {
    font-size: 20px; /* text-xl */
  }

  .time-display-vertical {
    flex-direction: row;
    gap: 8px;
  }

  /* 导航胶囊在移动端可滚动 */
  .nav-pill-container {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }
}
```

**Tablet/Desktop (≥ 768px)：**

```css
/* 保持默认横向布局 */
header {
  flex-direction: row;
}
```

#### 5.6.8 固定约束

**必须遵守的规则：**

1. **header 结构固定**：必须包含 Logo + 标题（左侧）和 时间 + 操作按钮（右侧）
2. **导航胶囊位置固定**：必须位于 header 下方，与更新时间/刷新按钮在同一行
3. **间距固定**：所有间距必须使用上述规范中的值，不得随意调整
4. **按钮样式固定**：圆形按钮必须使用 40px × 40px 尺寸，刷新按钮使用 32px × 32px
5. **导航胶囊高度固定**：容器高度固定为 40px，不得调整
6. **时间格式固定**：
   - 日期格式：YYYY-MM-DD
   - 时间格式：HH:MM:SS
   - 字体：使用 `var(--font-mono)`（栈见 §3.1：`SF Mono` / `Menlo` / `Monaco` / `Roboto Mono` / `Inconsolata` / `ui-monospace`）

**禁止的行为：**

- ❌ 不得更改 header 的 flex 布局方式
- ❌ 不得调整导航胶囊容器的圆角（必须为 9999px）
- ❌ 不得在导航胶囊项之间添加额外间距（必须为 1px）
- ❌ 不得更改圆形按钮的尺寸比例
- ❌ 不得在 header 中添加额外的装饰元素

### 5.7 进度条 (Progress Bar)

```css
.progress-bar {
  height: 6px;
  border-radius: 3px;
  background: var(--track-bg);
}

.progress-fill {
  height: 100%;
  border-radius: 3px;
}
```

### 5.8 排名徽章 (Rank Badge)

| 排名  | 渐变色              | 阴影                                 |
| ----- | ------------------- | ------------------------------------ |
| 第1名 | `#FFD700 → #FFA500` | `0 2px 8px rgba(255, 215, 0, 0.4)`   |
| 第2名 | `#C0C0C0 → #A8A8A8` | `0 2px 8px rgba(192, 192, 192, 0.4)` |
| 第3名 | `#CD7F32 → #B8860B` | `0 2px 8px rgba(205, 127, 50, 0.4)`  |

```css
.rank-badge {
  width: 28px;
  height: 28px;
  border-radius: 50%;
}
```

### 5.9 时间显示

#### 横向布局

```css
.time-display {
  font-size: 14px;
  font-weight: 500;
  font-family: var(--font-mono);
  gap: 10px;
}
```

#### 纵向布局

```css
.time-display-vertical {
  flex-direction: column;
  gap: 4px;
  font-family: var(--font-mono);
}
.time-display-vertical span:first-child {
  font-size: 11px;
  opacity: 0.8;
}
.time-display-vertical span:last-child {
  font-size: 13px;
  font-weight: 600;
}
```

---

## 6. 图表规范

### 6.1 图表容器

```css
.chart-shell {
  background: var(--chart-shell-bg);
  border: 1px solid var(--chart-shell-border);
  border-radius: 12px;
  padding: 16px;
  position: relative;
  z-index: 1;
}
```

**阴影遮挡约束：**

- ✅ 所有图表容器必须设置 `position: relative` 和 `z-index: 1`
- ✅ 确保图表容器不被其他卡片的阴影遮挡
- ✅ 与面板、卡片保持相同的 z-index 值

**背景透明度要求：**

- 浅色模式：`--chart-shell-bg: rgba(248, 251, 255, 0.98)` - 接近白色，透明度98%
- 深色模式：`--chart-shell-bg: rgba(15, 28, 55, 0.98)` - 深蓝色，透明度98%
- **禁止使用过深的背景色**，确保图表内容清晰可读
- 图表背景应与卡片背景保持协调，避免视觉突兀

### 6.2 暗纹背景（点阵）

图表容器仅保留**点阵**一层，使用 `--chart-grid` 控制疏密与对比；**不再叠加**横向网格线或第二套斜向/竖向纹理，以免与「去冗余装饰」原则冲突。

```css
.chart-shell-pattern {
  background-image: radial-gradient(circle at 1px 1px, var(--chart-grid) 1px, transparent 0);
  background-size: 20px 20px;
}
```

折线/面积系列颜色使用 `--chart-line-*` / `--chart-area-*`（见 §11）时，**不**为折线路径添加 SVG `filter` 发光；面积图**不**使用竖线 hatch 填充（除非产品另行要求并回本文件迭代）。

### 6.3 柱状图渐变

```css
.bar-gradient-1 {
  background: linear-gradient(180deg, #4d7cff 0%, #6fa8ff 100%);
}
.bar-gradient-2 {
  background: linear-gradient(180deg, #3b6cf5 0%, #5a9cff 100%);
}
.bar-gradient-3 {
  background: linear-gradient(180deg, #5a8dff 0%, #7cb5ff 100%);
}
```

### 6.4 半圆仪表盘

```css
.gauge-container {
  width: 200px;
  height: 120px;
}

.gauge-fill {
  stroke-dasharray: 251.2;
  stroke-dashoffset: 251.2;
}
```

**实现注意**：仪表盘容器与扇区描边使用 `--gauge-track` 等 Token 即可；**不要**对整块 gauge 容器加 `filter: drop-shadow(...)`，避免与扁平玻璃风格不一致。

---

## 7. 动画规范

### 7.1 过渡时间

| 类型       | 时长        | 缓动函数                             |
| ---------- | ----------- | ------------------------------------ |
| 快速       | 0.2s        | ease                                 |
| 标准       | 0.3s        | ease                                 |
| 主题切换   | 0.4s - 0.5s | ease                                 |
| 仪表盘填充 | 2s          | cubic-bezier(0.25, 0.46, 0.45, 0.94) |
| 进度条动画 | 1s          | ease-out                             |
| 卡片加载   | 0.5s        | ease-out                             |

### 7.2 动画效果

#### 卡片加载动画 (fadeInUp)（可选）

页面加载时，卡片可从下方淡入上浮并按顺序出现；**replica 工程可不绑定** `.panel` 的 `fadeInUp`，避免首屏动效干扰运维阅读。若静态 HTML 保留，延迟阶梯可与下表一致：

```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.panel {
  animation: fadeInUp 0.5s ease-out forwards;
  opacity: 0;
}

.panel:nth-child(1) {
  animation-delay: 0.05s;
}
.panel:nth-child(2) {
  animation-delay: 0.1s;
}
.panel:nth-child(3) {
  animation-delay: 0.15s;
}
.panel:nth-child(4) {
  animation-delay: 0.2s;
}
.panel:nth-child(5) {
  animation-delay: 0.25s;
}
.panel:nth-child(6) {
  animation-delay: 0.3s;
}
.panel:nth-child(7) {
  animation-delay: 0.35s;
}
.panel:nth-child(8) {
  animation-delay: 0.4s;
}
```

#### 仪表盘填充动画

半圆环形和圆形仪表盘的填充动画：

```css
/* 大型半圆环动画 */
.gauge-fill {
  stroke-dashoffset: 251.2;
  transition: stroke-dashoffset 2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

/* 小型半圆环动画 */
.gauge-fill-small {
  stroke-dashoffset: 125.6;
  transition: stroke-dashoffset 2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

/* 圆环动画 */
.circle-fill {
  stroke-dashoffset: 251.2;
  transition: stroke-dashoffset 2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}
```

**动画触发逻辑：**

1. 先保存所有目标值（stroke-dashoffset）
2. 重置到初始状态（251.2 或 125.6）
3. 强制重绘（`void document.body.offsetHeight`）
4. 使用 `requestAnimationFrame` 确保动画正确触发
5. 恢复 transition 并设置目标值

#### 进度条动画

进度条按顺序从 0% 开始填充：

```css
.progress-fill {
  transition: width 1s ease-out;
}
```

**动画触发逻辑：**

1. 先保存所有进度条的目标宽度
2. 设置 transition 为 none，宽度设为 0%
3. 强制重绘
4. 按顺序（每条间隔 100ms）恢复 transition 并设置目标宽度

```javascript
// 触发进度条动画 - 按顺序逐个动画
const progressBars = document.querySelectorAll(".progress-fill");
const progressWidths = [];

// 先保存所有宽度
progressBars.forEach((el, index) => {
  progressWidths[index] = el.style.width;
});

// 设置为0
progressBars.forEach((el, index) => {
  el.style.transition = "none";
  el.style.width = "0%";
});

// 强制重绘
void document.body.offsetHeight;

// 恢复transition并按顺序动画
progressBars.forEach((el, index) => {
  setTimeout(() => {
    el.style.transition = "width 1s ease-out";
    el.style.width = progressWidths[index];
  }, index * 100);
});
```

#### 横向条形图动画

Token 使用量的横向条形图分段动画：

```css
.bar-segment {
  transition: all 1.5s ease-out;
}
```

**动画触发逻辑：**

```javascript
document.querySelectorAll(".bar-segment").forEach((el, index) => {
  const width = el.style.width;
  el.style.width = "0";
  setTimeout(
    () => {
      el.style.width = width;
    },
    50 + index * 100,
  );
});
```

#### 图表容器动画

图表容器（chart-shell）的入场动画：

```css
.chart-shell {
  animation: fadeInUp 0.6s ease-out;
}
```

**背景统一要求：**

- 图表容器背景必须与卡片背景一致：`var(--bg-card)`
- 禁止使用过深的背景色
- 确保图表内容清晰可读

#### 刷新旋转动画

```css
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.refresh-btn.spinning {
  animation: spin 1s linear;
}
```

#### 卡片悬停效果（可选）

原型页可使用轻微上浮；**replica 工程当前未启用** `.panel:hover` 位移动画，以保持界面稳定。若启用，需保证与 §1.3 一致且不增加额外阴影 Token。

```css
/* 可选 — 非强制 */
.panel:hover {
  transform: translateY(-2px);
  transition: transform 0.3s ease;
}
```

### 7.3 主题切换动画

切换主题时，所有动画重新触发，与页面刷新效果一致：

```javascript
function toggleTheme() {
  // 切换主题类
  const body = document.body;
  if (body.classList.contains("theme-light")) {
    body.classList.remove("theme-light");
    body.classList.add("theme-dark");
  } else {
    body.classList.remove("theme-dark");
    body.classList.add("theme-light");
  }

  // 更新图表颜色
  updateCharts();

  // 延迟触发动画，确保主题切换完成
  setTimeout(() => {
    triggerAnimations();
  }, 100);
}
```

### 7.4 动画触发函数

统一的动画触发函数，用于页面加载和主题切换：

```javascript
function triggerAnimations() {
  // 1. 保存所有目标值
  // 2. 重置到初始状态
  // 3. 强制重绘
  // 4. 触发卡片浮动动画
  // 5. 触发仪表盘填充动画
  // 6. 触发进度条动画
}
```

### 7.5 动画时序

| 动画类型   | 延迟          | 持续时间 | 间隔  |
| ---------- | ------------- | -------- | ----- |
| 卡片浮动   | 0.05s × index | 0.5s     | 0.05s |
| 仪表盘填充 | 0             | 2s       | -     |
| 进度条填充 | 0.1s × index  | 1s       | 0.1s  |

**动画顺序：**

1. 卡片开始浮动（按顺序）
2. 仪表盘开始填充（同时）
3. 进度条开始填充（按顺序）

### 7.6 性能优化

- 使用 `requestAnimationFrame` 确保动画流畅
- 使用 `void document.body.offsetHeight` 强制重绘
- 避免在动画过程中修改 DOM 结构
- 使用 CSS transition 而非 JavaScript 动画

---

## 8. 布局规范

### 8.1 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  Header (Logo + 标题 + 导航胶囊 + 时间 + 操作按钮)           │
├─────────────────────────────────────────────────────────────┤
│  更新时间 + 刷新按钮                                         │
├─────────────────────────────────────────────────────────────┤
│  指标卡片区域 (4列网格)                                      │
├─────────────────────────────────────────────────────────────┤
│  图表区域 (多列网格)                                         │
├─────────────────────────────────────────────────────────────┤
│  其他内容区域                                                │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 响应式断点

| 断点 | 宽度           | 列数  |
| ---- | -------------- | ----- |
| sm   | < 768px        | 1列   |
| md   | 768px - 1024px | 2列   |
| lg   | > 1024px       | 3-4列 |

### 8.3 最大宽度

```css
.max-w-7xl {
  max-width: 1280px;
  margin: 0 auto;
}
```

### 8.4 主内容容器与卡片对齐（重要）

主内容区应使用**单一** `max-w-7xl mx-auto`（或等价类名）包裹从首屏卡片到页尾的全部 `.panel` / 栅格列。**禁止**在某一卡片（如「Notebook 开发环境」）结束处多写一个 `</div>` 而提前关闭该容器，否则后续「在线服务列表」等块会落到容器外，表现为**更宽、左侧与上方卡片不对齐**。

**正确结构示意：**

```html
<main class="max-w-7xl mx-auto px-6 pb-10 space-y-4">
  <!-- 多个 section，内部再套 grid / panel -->
  <section>...</section>
  <section class="panel ...">...</section>
  <!-- Notebook -->
  <section class="panel ...">...</section>
  <!-- 在线服务，与上同宽 -->
</main>
```

排查时请在开发者工具中确认：`max-w-7xl` 的闭合标签**只出现一次**，且位于主内容末尾。

---

## 9. Logo 规范

### 9.1 Logo 样式

六边形矢量图形（如 `BrandHexLogo`），置于 `.brand-mark` 内；描边/填充使用 `--brand-mark-border`、`--brand-mark-color`。**不强制**外圈圆形光晕层；若需背景，仅用上述 Token 的弱对比底，避免额外未文档化光斑。

```css
.logo-icon svg {
  width: 40px;
  height: 40px;
}
```

### 9.2 Logo 渐变

```css
linear-gradient(0% → 100%, #4d7cff → #2F56C2)
```

---

## 10. CSS 变量完整清单

以下为**推荐的 CSS 自定义属性结构**，可与设计 Token（如 Figma Variables）或各框架主题系统映射；**`:root`** 中为跨主题语义色与 `--chart-series-*`；**`body.theme-light` / `body.theme-dark`**（或等价选择器）内为随主题切换的表面色与图表派生色。实现方可调整文件组织方式，**变量语义与本表一致即可**。

### 10.1 `:root`（全局）

```css
:root {
  --font-ui:
    -apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "Noto Sans SC",
    "Source Han Sans SC", Arial, sans-serif;
  --font-data-display: "Roboto", Arial, sans-serif;
  --font-mono: "SF Mono", "Menlo", "Monaco", "Roboto Mono", "Inconsolata", ui-monospace, monospace;
  --nav-pill-active-bg: #2775ff;
  --nav-pill-active-text: #ffffff;
  --success: #4be38e;
  --warning: #ffbf5b;
  --danger: #ff7a82;
  --accent-cyan: #60d8ff;
  --accent-teal: #5fd7bc;
  --accent-yellow: #ffd06a;
  /* 以下为 :root 默认序列色；body.theme-* 内会再次赋值以对齐 §2.6 */
  --chart-series-1: #79aaff;
  --chart-series-2: #5fd7bc;
  --chart-series-3: #60d8ff;
  --chart-series-4: #ffd06a;
  --chart-series-5: #d2a8ff;
  --chart-series-6: #ff7a82;
  --chart-series-7: #8d96ff;
  --chart-series-8: #7ce7a2;
  --chart-series-9: #ff9f5f;
  --chart-series-10: #ff8ec7;
}
```

### 10.2 浅色模式变量 `body.theme-light`（节选）

```css
body.theme-light {
  --bg-app: #edf4ff;
  --bg-card: rgba(255, 255, 255, 0.98);
  --bg-card-soft: rgba(248, 251, 255, 0.98);
  --bg-chip: rgba(229, 237, 250, 0.92);
  --text-primary: #16233a;
  --text-secondary: #4d6587;
  --text-muted: #7d90ac;
  --accent: #2f6df6;
  --page-bg-start: #edf4ff;
  --page-bg-mid: #f7fbff;
  --page-bg-end: #e7f0ff;
  --panel-surface: rgba(255, 255, 255, 0.98);
  --line: rgba(126, 155, 208, 0.24);
  --line-strong: rgba(84, 126, 228, 0.28);
  --shadow-panel: 0 24px 56px rgba(40, 72, 120, 0.14), inset 0 1px 0 rgba(255, 255, 255, 0.8);
  --metric-shadow: 0 16px 32px rgba(59, 90, 138, 0.12), inset 0 1px 0 rgba(255, 255, 255, 0.78);
  --button-border: rgba(123, 154, 211, 0.22);
  --button-bg: rgba(255, 255, 255, 0.86);
  --button-bg-hover: rgba(235, 243, 255, 0.98);
  --brand-mark-border: rgba(95, 131, 220, 0.2);
  --brand-mark-color: #2459dd;
  --subcard-border: rgba(126, 155, 208, 0.16);
  --subcard-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.94);
  --chart-shell-bg: rgba(248, 251, 255, 0.98);
  --chart-shell-border: rgba(121, 151, 214, 0.18);
  --chart-grid: rgba(125, 151, 205, 0.12);
  --track-bg: rgba(82, 113, 165, 0.12);
  --gauge-track: rgba(176, 191, 220, 0.68);
  --chart-area-start: rgba(118, 166, 255, 0.28);
  --chart-area-end: rgba(79, 117, 207, 0.02);
  --chart-line-start: #4d7cff;
  --chart-line-end: #28b6e5;
  --chart-point-fill: #ffffff;
  --chart-point-stroke: #4d7df1;
  /* 图表色板 - 浅色模式低饱和度/静音化 */
  --chart-series-1: #b7caff;
  --chart-series-2: #a0e6cf;
  --chart-series-3: #c7eecc;
  --chart-series-4: #ffea94;
  --chart-series-5: #cbbaba;
  --chart-series-6: #e8b7bd;
  --chart-series-7: #b9bee0;
  --chart-series-8: #b4ccb9;
  --chart-series-9: #e8d3b7;
  --chart-series-10: #d3bfd1;
  /* chip-active-*、link-pill-*、clock-*、health-ring-*、rank-* 等见工程全量 */
}
```

### 10.3 深色模式变量 `body.theme-dark`（节选）

```css
body.theme-dark {
  --bg-app: #050d18;
  --bg-card: rgba(22, 42, 78, 1);
  --bg-card-soft: rgba(20, 38, 72, 1);
  --bg-chip: rgba(34, 56, 100, 0.52);
  --text-primary: #f3f7ff;
  --text-secondary: #c5d3ef;
  --text-muted: #7386af;
  --accent: #79aaff;
  --page-bg-start: #0a1530;
  --page-bg-mid: #050b15;
  --page-bg-end: #040912;
  --panel-surface: rgba(18, 35, 65, 1);
  --line: rgba(100, 147, 255, 0.18);
  --line-strong: rgba(121, 169, 255, 0.34);
  --shadow-panel: 0 24px 60px rgba(0, 0, 0, 0.34), inset 0 1px 0 rgba(255, 255, 255, 0.06);
  --metric-shadow: 0 18px 36px rgba(0, 0, 0, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.04);
  --button-border: rgba(112, 149, 235, 0.16);
  --button-bg: rgba(17, 32, 61, 0.56);
  --button-bg-hover: rgba(31, 54, 97, 0.76);
  --brand-mark-border: rgba(151, 184, 255, 0.28);
  --brand-mark-color: #f5f7ff;
  --subcard-border: rgba(112, 148, 233, 0.12);
  --subcard-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.03);
  --chart-shell-bg: rgba(15, 28, 55, 0.98);
  --chart-shell-border: rgba(111, 151, 239, 0.12);
  --chart-grid: rgba(112, 149, 233, 0.08);
  --track-bg: rgba(255, 255, 255, 0.06);
  --gauge-track: rgba(74, 98, 145, 0.36);
  --chart-area-start: rgba(149, 201, 255, 0.56);
  --chart-area-end: rgba(66, 98, 173, 0.08);
  --chart-line-start: #66a8ff;
  --chart-line-end: #a8f1ff;
  --chart-point-fill: #d3f5ff;
  --chart-point-stroke: #7db7ff;
  /* 图表色板 - 深色模式 */
  --chart-series-1: #79aaff;
  --chart-series-2: #5fd7bc;
  --chart-series-3: #60d8ff;
  --chart-series-4: #ffd06a;
  --chart-series-5: #d2a8ff;
  --chart-series-6: #ff7a82;
  --chart-series-7: #8d96ff;
  --chart-series-8: #7ce7a2;
  --chart-series-9: #ff9f5f;
  --chart-series-10: #ff8ec7;
  /* 其余同 10.2 说明 */
}
```

### 10.4 清单与 §2 表格的对应关系

- §2.2 / §2.3 中的 `--bg-card`、`--panel-surface`、`--line` 等与上表一致；**深色 `--metric-shadow`** 以 §2.5 表格与 **§10.3** 为准（`0 18px 36px …`）。
- 图表折线/面积/点样式优先使用 `--chart-line-*`、`--chart-area-*`、`--chart-point-*`，与 §6 呼应。
- 导航胶囊选中态优先使用 `--nav-pill-active-bg` / `--nav-pill-active-text`（见 §2.3、§5.5）。

---

## 11. 设计原则

### 11.1 一致性

- 统一使用 CSS 变量管理颜色
- 保持组件样式的一致性
- 遵循既定的间距规范

### 11.2 可访问性

- 确保文字与背景的对比度
- 提供清晰的视觉反馈
- 支持主题切换适应不同环境

### 11.3 性能优化

- 使用 CSS 变量减少重复代码
- 合理使用过渡动画
- 避免过度使用阴影和模糊效果

### 11.4 文档与代码同步

- 视觉或 Token 变更时，应同步更新本规范与产品内的主题源（CSS 变量、设计库等），并在 **附录 A** 或变更说明中**一句话写清动机**。
- 新增装饰性效果前，先归入 §1.3「采用/不采用」表，避免页面与文档再次分叉。

---

## 12. 移动端适配标准

### 12.1 响应式断点

| 断点名称 | 屏幕宽度        | 设备类型    | Tailwind 类前缀 |
| -------- | --------------- | ----------- | --------------- |
| Mobile S | < 375px         | 小屏手机    | 默认（无前缀）  |
| Mobile M | 375px - 424px   | 中屏手机    | `sm:`           |
| Mobile L | 425px - 767px   | 大屏手机    | `sm:`           |
| Tablet   | 768px - 1023px  | iPad / 平板 | `md:`           |
| Laptop   | 1024px - 1439px | 笔记本      | `lg:`           |
| Desktop  | ≥ 1440px        | 桌面显示器  | `xl:`           |

**断点实现：**

```css
/* Tailwind CSS 默认断点 */
sm: 640px   /* 小屏手机横屏 */
md: 768px   /* iPad 竖屏 */
lg: 1024px  /* iPad 横屏 / 小笔记本 */
xl: 1280px  /* 桌面显示器 */
2xl: 1536px /* 大屏显示器 */
```

### 12.2 布局适配规则

#### 12.2.1 主容器宽度

| 断点    | 容器宽度    | 左右边距 |
| ------- | ----------- | -------- |
| Mobile  | 100%        | 16px     |
| Tablet  | 100%        | 24px     |
| Desktop | 最大 1440px | 32px     |

```css
.main-container {
  width: 100%;
  padding: 0 16px; /* Mobile */
}

@media (min-width: 768px) {
  .main-container {
    padding: 0 24px; /* Tablet */
  }
}

@media (min-width: 1024px) {
  .main-container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 32px; /* Desktop */
  }
}
```

#### 12.2.2 栅格系统适配

| 断点    | 栅格列数 | 卡片布局 |
| ------- | -------- | -------- |
| Mobile  | 1列      | 单列堆叠 |
| Tablet  | 2列      | 双列并排 |
| Desktop | 3-4列    | 多列并排 |

**实现示例：**

```html
<!-- 3列卡片布局 -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  <div class="panel">卡片 1</div>
  <div class="panel">卡片 2</div>
  <div class="panel">卡片 3</div>
</div>

<!-- 4列卡片布局 -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
  <div class="panel">卡片 1</div>
  <div class="panel">卡片 2</div>
  <div class="panel">卡片 3</div>
  <div class="panel">卡片 4</div>
</div>
```

### 12.3 图表适配策略

#### 12.3.1 图表展示规则

| 屏幕宽度 | 图表处理方式 | 说明                   |
| -------- | ------------ | ---------------------- |
| ≥ 768px  | 显示图表     | 正常展示图表           |
| < 768px  | 文字替代     | 隐藏图表，展示文字数据 |

**实现原则：**

- 图表容器在小屏时隐藏（`hidden md:block`）
- 文字数据在小屏时显示（`block md:hidden`）
- 文字数据需包含图表的核心信息

#### 12.3.2 各类图表的文字替代方案

**1. 半圆环图（Gauge Chart）**

```html
<!-- 桌面端：显示图表 -->
<div class="hidden md:flex justify-center">
  <svg class="gauge-chart">...</svg>
</div>

<!-- 移动端：显示文字 -->
<div class="block md:hidden text-center">
  <div class="text-3xl font-bold text-primary">70%</div>
  <div class="text-sm text-muted">CPU 平均利用率</div>
</div>
```

**2. 折线图（Line Chart）**

```html
<!-- 桌面端：显示图表 -->
<div class="hidden md:block chart-shell">
  <canvas id="trendChart"></canvas>
</div>

<!-- 移动端：显示关键数据 -->
<div class="block md:hidden space-y-3">
  <div
    class="flex justify-between items-center p-3 rounded-lg"
    style="background: var(--chart-shell-bg);"
  >
    <span class="text-muted">当前值</span>
    <span class="text-xl font-bold text-primary">1,234</span>
  </div>
  <div
    class="flex justify-between items-center p-3 rounded-lg"
    style="background: var(--chart-shell-bg);"
  >
    <span class="text-muted">峰值</span>
    <span class="text-xl font-bold text-primary">2,567</span>
  </div>
  <div
    class="flex justify-between items-center p-3 rounded-lg"
    style="background: var(--chart-shell-bg);"
  >
    <span class="text-muted">平均值</span>
    <span class="text-xl font-bold text-primary">1,890</span>
  </div>
  <div
    class="flex justify-between items-center p-3 rounded-lg"
    style="background: var(--chart-shell-bg);"
  >
    <span class="text-muted">趋势</span>
    <span class="text-green-400">↑ 12.5%</span>
  </div>
</div>
```

**3. 饼图（Pie Chart）**

```html
<!-- 桌面端：显示图表 -->
<div class="hidden md:flex flex-col items-center">
  <canvas id="pieChart"></canvas>
  <div class="legend">...</div>
</div>

<!-- 移动端：显示列表 -->
<div class="block md:hidden space-y-2">
  <div
    class="flex items-center justify-between p-3 rounded-lg"
    style="background: var(--chart-shell-bg);"
  >
    <div class="flex items-center gap-2">
      <div class="w-3 h-3 rounded-full" style="background: #6b9aff;"></div>
      <span class="text-primary">大模型微调</span>
    </div>
    <span class="font-bold text-primary">18</span>
  </div>
  <div
    class="flex items-center justify-between p-3 rounded-lg"
    style="background: var(--chart-shell-bg);"
  >
    <div class="flex items-center gap-2">
      <div class="w-3 h-3 rounded-full" style="background: #4cc9a8;"></div>
      <span class="text-primary">宝塔</span>
    </div>
    <span class="font-bold text-primary">14</span>
  </div>
  <!-- 更多项目... -->
</div>
```

**4. 柱状图（Bar Chart）**

```html
<!-- 桌面端：显示图表 -->
<div class="hidden md:block chart-shell">
  <canvas id="barChart"></canvas>
</div>

<!-- 移动端：显示排行列表 -->
<div class="block md:hidden space-y-2">
  <div class="flex items-center gap-3">
    <div class="rank-badge rank-1">1</div>
    <div class="flex-1">
      <div class="flex justify-between mb-1">
        <span class="text-primary">项目 A</span>
        <span class="text-secondary">256</span>
      </div>
      <div class="progress-bar">
        <div class="progress-fill tone-blue" style="width: 85%;"></div>
      </div>
    </div>
  </div>
  <!-- 更多排行... -->
</div>
```

**5. 横向条形图（Horizontal Bar）**

```html
<!-- 桌面端：显示图表 -->
<div class="hidden md:block">
  <div class="h-12 rounded overflow-hidden flex">
    <div style="width: 69.3%; background: #4d7cff;"></div>
    <div style="width: 30.7%; background: #93c5fd;"></div>
  </div>
</div>

<!-- 移动端：显示分段数据 -->
<div class="block md:hidden space-y-2">
  <div class="p-3 rounded-lg" style="background: var(--chart-shell-bg);">
    <div class="flex justify-between items-center mb-2">
      <span class="text-muted">输入 Token</span>
      <span class="text-primary font-bold">856,234</span>
    </div>
    <div class="progress-bar">
      <div class="progress-fill tone-blue" style="width: 69.3%;"></div>
    </div>
  </div>
  <div class="p-3 rounded-lg" style="background: var(--chart-shell-bg);">
    <div class="flex justify-between items-center mb-2">
      <span class="text-muted">输出 Token</span>
      <span class="text-primary font-bold">378,333</span>
    </div>
    <div class="progress-bar">
      <div class="progress-fill" style="width: 30.7%; background: #93c5fd;"></div>
    </div>
  </div>
</div>
```

#### 12.3.3 图表文字替代的数据要求

| 图表类型   | 必须展示的数据       | 可选数据       |
| ---------- | -------------------- | -------------- |
| 半圆环图   | 当前值、单位、标题   | 目标值、趋势   |
| 折线图     | 当前值、峰值、平均值 | 趋势、时间范围 |
| 饼图       | 各项名称、数值、占比 | 排名、变化     |
| 柱状图     | 排名、名称、数值     | 百分比、趋势   |
| 横向条形图 | 各段名称、数值       | 百分比、总计   |

### 12.4 字体大小适配

#### 12.4.1 标题字体

| 元素       | Mobile | Tablet | Desktop |
| ---------- | ------ | ------ | ------- |
| 页面标题   | 20px   | 24px   | 28px    |
| 卡片标题   | 16px   | 18px   | 18px    |
| 数据大数字 | 24px   | 28px   | 32px    |

**实现示例：**

```html
<h1 class="text-xl md:text-2xl lg:text-3xl font-bold text-primary">通智算集群监控中心</h1>

<h2 class="text-base md:text-lg font-semibold text-primary">卡片标题</h2>

<div class="text-2xl md:text-3xl lg:text-4xl font-bold text-primary">1,234</div>
```

#### 12.4.2 正文字体

| 元素     | Mobile | Tablet | Desktop |
| -------- | ------ | ------ | ------- |
| 正文     | 14px   | 14px   | 14px    |
| 辅助文字 | 12px   | 12px   | 12px    |
| 表格文字 | 12px   | 12px   | 12px    |

### 12.5 间距适配

#### 12.5.1 卡片间距

| 断点    | 卡片间距 | 卡片内边距 |
| ------- | -------- | ---------- |
| Mobile  | 12px     | 16px       |
| Tablet  | 16px     | 20px       |
| Desktop | 16px     | 24px       |

**实现示例：**

```html
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3 md:gap-4">
  <div class="panel rounded-2xl p-4 md:p-5 lg:p-6">卡片内容</div>
</div>
```

#### 12.5.2 页面边距

| 断点    | 页面顶部 | 页面底部 | 页面左右 |
| ------- | -------- | -------- | -------- |
| Mobile  | 16px     | 24px     | 16px     |
| Tablet  | 24px     | 32px     | 24px     |
| Desktop | 32px     | 40px     | 32px     |

### 12.6 导航适配

#### 12.6.1 顶部导航

| 断点    | 导航样式 | 说明               |
| ------- | -------- | ------------------ |
| Mobile  | 汉堡菜单 | 收起导航，点击展开 |
| Tablet  | 横向导航 | 显示主要导航项     |
| Desktop | 横向导航 | 显示所有导航项     |

**实现示例：**

```html
<!-- Mobile: 汉堡菜单按钮 -->
<button class="md:hidden p-2" onclick="toggleMobileMenu()">
  <svg class="w-6 h-6">...</svg>
</button>

<!-- Mobile: 展开的导航菜单 -->
<div class="hidden md:hidden" id="mobileMenu">
  <div class="flex flex-col space-y-2 p-4">
    <a href="#" class="nav-link">利用率热力图</a>
    <a href="#" class="nav-link">单卡明细</a>
    <a href="#" class="nav-link">租户ROI</a>
  </div>
</div>

<!-- Tablet/Desktop: 横向导航 -->
<div class="hidden md:flex items-center gap-2">
  <button class="nav-pill active">利用率热力图</button>
  <button class="nav-pill">单卡明细</button>
  <button class="nav-pill">租户ROI</button>
</div>
```

#### 12.6.2 导航胶囊（Nav Pill）

| 断点    | 胶囊大小    | 间距 |
| ------- | ----------- | ---- |
| Mobile  | 12px / 20px | 4px  |
| Tablet  | 12px / 20px | 4px  |
| Desktop | 12px / 20px | 4px  |

```css
.nav-pill {
  padding: 6px 12px;
  font-size: 12px;
  border-radius: 20px;
}

.nav-pill.active {
  background: var(--nav-pill-active-bg);
  color: var(--nav-pill-active-text);
}
```

### 12.7 表格适配

#### 12.7.1 表格处理策略

| 断点    | 表格展示方式 | 说明                 |
| ------- | ------------ | -------------------- |
| Desktop | 完整表格     | 显示所有列           |
| Tablet  | 横向滚动     | 保留表格，可横向滚动 |
| Mobile  | 卡片列表     | 转换为卡片形式       |

**实现示例：**

```html
<!-- Desktop/Tablet: 表格 -->
<div class="hidden md:block overflow-x-auto">
  <table class="w-full">
    <thead>
      <tr>
        <th>模型</th>
        <th>QPS</th>
        <th>延迟</th>
        <th>状态</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>MiniMax-M2.1</td>
        <td>0.20</td>
        <td>1153ms</td>
        <td><span class="status-chip status-success">运行中</span></td>
      </tr>
    </tbody>
  </table>
</div>

<!-- Mobile: 卡片列表 -->
<div class="block md:hidden space-y-2">
  <div class="p-3 rounded-lg" style="background: var(--chart-shell-bg);">
    <div class="flex justify-between items-center mb-2">
      <span class="font-medium text-primary">MiniMax-M2.1</span>
      <span class="status-chip status-success">运行中</span>
    </div>
    <div class="grid grid-cols-2 gap-2 text-sm">
      <div>
        <span class="text-muted">QPS:</span>
        <span class="text-primary">0.20</span>
      </div>
      <div>
        <span class="text-muted">延迟:</span>
        <span class="text-primary">1153ms</span>
      </div>
    </div>
  </div>
</div>
```

#### 12.7.2 表格列优先级

当屏幕宽度不足时，按以下优先级隐藏列：

| 优先级 | 列类型    | 说明     |
| ------ | --------- | -------- |
| P0     | 名称/标识 | 始终显示 |
| P1     | 状态      | 始终显示 |
| P2     | 核心指标  | 重要数据 |
| P3     | 次要指标  | 可隐藏   |
| P4     | 辅助信息  | 优先隐藏 |

### 12.8 卡片适配

#### 12.8.1 特殊卡片处理

**1. 摩尔线程 S4000（5列布局）**

```html
<!-- Desktop: 5列 -->
<div class="hidden lg:grid grid-cols-5 gap-4">
  <!-- GPU利用率、显存利用率、温度功率、时钟频率、运行进程 -->
</div>

<!-- Tablet: 3列 + 2列 -->
<div class="hidden md:grid lg:hidden grid-cols-3 gap-4 mb-4">
  <!-- 前3个指标 -->
</div>
<div class="hidden md:grid lg:hidden grid-cols-2 gap-4">
  <!-- 后2个指标 -->
</div>

<!-- Mobile: 单列堆叠 -->
<div class="block md:hidden space-y-3">
  <div class="p-4 rounded-xl" style="background: var(--chart-shell-bg);">
    <div class="text-muted text-sm mb-2">GPU 利用率</div>
    <div class="text-2xl font-bold text-primary">75%</div>
  </div>
  <!-- 更多指标... -->
</div>
```

**2. Token 使用量（横向条形图）**

```html
<!-- Desktop: 横向条形图 -->
<div class="hidden md:block">
  <div class="h-12 rounded overflow-hidden flex">
    <div style="width: 69.3%; background: #4d7cff;"></div>
    <div style="width: 30.7%; background: #93c5fd;"></div>
  </div>
</div>

<!-- Mobile: 分段数据卡片 -->
<div class="block md:hidden space-y-2">
  <div class="p-3 rounded-lg" style="background: var(--chart-shell-bg);">
    <div class="text-muted text-xs mb-1">输入 Token</div>
    <div class="text-xl font-bold text-primary">856,234</div>
    <div class="text-xs text-muted">69.3%</div>
  </div>
  <div class="p-3 rounded-lg" style="background: var(--chart-shell-bg);">
    <div class="text-muted text-xs mb-1">输出 Token</div>
    <div class="text-xl font-bold text-primary">378,333</div>
    <div class="text-xs text-muted">30.7%</div>
  </div>
</div>
```

**3. 节点资源 Top10（排行列表）**

```html
<!-- Desktop/Tablet: 显示前5名 -->
<div class="hidden md:block space-y-3">
  <!-- 前5名数据 -->
</div>

<!-- Mobile: 显示前3名 + 查看全部按钮 -->
<div class="block md:hidden space-y-3">
  <!-- 前3名数据 -->
  <button class="w-full text-center text-sm text-accent py-2">查看全部 (共10项)</button>
</div>
```

### 12.9 按钮与交互适配

#### 12.9.1 按钮大小

| 断点    | 按钮高度 | 按钮内边距 |
| ------- | -------- | ---------- |
| Mobile  | 36px     | 8px 16px   |
| Tablet  | 40px     | 10px 20px  |
| Desktop | 40px     | 10px 20px  |

```css
.btn {
  height: 36px;
  padding: 8px 16px;
}

@media (min-width: 768px) {
  .btn {
    height: 40px;
    padding: 10px 20px;
  }
}
```

#### 12.9.2 时间范围按钮组

```html
<!-- Desktop: 横向排列 -->
<div class="hidden md:flex gap-1">
  <button class="time-btn active">30m</button>
  <button class="time-btn">1h</button>
  <button class="time-btn">6h</button>
  <button class="time-btn">24h</button>
</div>

<!-- Mobile: 下拉选择 -->
<div class="block md:hidden">
  <select class="w-full p-2 rounded-lg" style="background: var(--chart-shell-bg);">
    <option value="30m">30分钟</option>
    <option value="1h">1小时</option>
    <option value="6h">6小时</option>
    <option value="24h">24小时</option>
  </select>
</div>
```

### 12.10 隐藏与显示规则

#### 12.10.1 Tailwind CSS 响应式类

| 类名        | 说明         |
| ----------- | ------------ |
| `hidden`    | 所有屏幕隐藏 |
| `block`     | 所有屏幕显示 |
| `md:hidden` | ≥768px 隐藏  |
| `md:block`  | ≥768px 显示  |
| `lg:hidden` | ≥1024px 隐藏 |
| `lg:block`  | ≥1024px 显示 |

#### 12.10.2 常用组合

```html
<!-- 仅在 Mobile 显示 -->
<div class="block md:hidden">移动端内容</div>

<!-- 仅在 Tablet/Desktop 显示 -->
<div class="hidden md:block">桌面端内容</div>

<!-- 仅在 Desktop 显示 -->
<div class="hidden lg:block">大屏内容</div>

<!-- Mobile 单列, Tablet 双列, Desktop 三列 -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">...</div>
```

### 12.11 移动端性能优化

#### 12.11.1 图表渲染优化

- 移动端不渲染 Chart.js 图表，减少 JavaScript 执行
- 使用 CSS 隐藏而非 JavaScript 条件渲染
- 文字替代方案使用纯 HTML/CSS，无需额外 JavaScript

#### 12.11.2 图片与资源

- 使用响应式图片（`srcset`）
- 移动端加载小尺寸图标
- 延迟加载非关键资源

#### 12.11.3 触摸交互

- 按钮最小点击区域：44px × 44px
- 卡片点击区域：整卡可点击
- 避免使用 hover 状态作为主要交互方式

### 12.12 测试检查清单

#### 12.12.1 断点测试

- [ ] 320px - 375px（小屏手机）
- [ ] 375px - 424px（中屏手机）
- [ ] 424px - 767px（大屏手机）
- [ ] 768px - 1023px（iPad 竖屏）
- [ ] 1024px - 1439px（iPad 横屏 / 小笔记本）
- [ ] ≥1440px（桌面显示器）

#### 12.12.2 功能测试

- [ ] 所有图表在移动端有文字替代
- [ ] 表格在移动端可读或转换为卡片
- [ ] 导航在移动端可用
- [ ] 按钮可点击（触摸友好）
- [ ] 文字大小可读
- [ ] 间距合理，不拥挤

#### 12.12.3 内容测试

- [ ] 关键数据在移动端可见
- [ ] 文字替代数据准确
- [ ] 无横向滚动（除表格外）
- [ ] 无内容截断

---

## 13. 交付自检约束（强制）

> 以下约束适用于静态原型、工程实现及设计还原输出；交付前必须逐项核对。

### 约束 1：卡片间距必须符合设计规范

- **标准间距**：模块与卡片之间的外边距 / 栅格间隙统一为 **16px**（与 §4.3、§4.4 及 Tailwind `gap-4` / 等价 token 一致）。
- **不允许间距不一致**：同一页面内不得混用 12px、20px 等非规范节奏作为卡片级间距（除非规范另有章节单独定义）。
- **必须使用统一变量**：间距应通过设计 token / CSS 变量 / 主题级 spacing 工具类实现，**禁止**在组件内随意写死像素值替代整页节奏。

### 约束 2：卡片之间不得有阴影或遮挡

- **阴影不得重叠**：多卡片并列时，阴影仅作为单卡边缘层次表达，不得因 `z-index`、过大 `spread` 或层叠顺序导致邻卡阴影「糊成一片」或互相切割内容区（与 §5.2、§6.1、V2.2 修订中 z-index 约束一致）。
- **视觉边界清晰**：每张卡片的可点击区域与内容边界可读，不被相邻模块阴影误判为同一容器。
- **无干扰元素**：禁止额外装饰层（未在规范中定义的悬浮光斑、重复投影等）破坏卡片边界识别。

### 约束 3：输出前必须检查（间距 · 阴影 · 布局 · 视觉）

**间距检查清单**

- [ ] 卡片与卡片、段落与段落之间是否为 **16px**（或规范明确的其他档位且全页统一）。
- [ ] 卡片内边距是否为 **24px（p-6）** 或与 §4.5 / `.panel` 约定一致。
- [ ] 标题与内容块之间是否为 **16px（mb-4）**。

**阴影检查清单**

- [ ] 面板是否仅使用 `--shadow-panel`（或规范等价变量），指标类是否使用 `--metric-shadow`。
- [ ] 是否存在阴影叠压导致内容发灰、发脏或边界不清。
- [ ] `position` / `z-index` 是否满足「单卡一层、不恶意堆叠」的约定。

**布局检查清单**

- [ ] 栅格类型是否仅使用规范允许的段落（如单列 / 双列 / 三联 / 指标网格等），`gap` 是否为 **16px**。
- [ ] 小屏下是否无强制横向滚动才能读完主模块（除表格等例外）。
- [ ] 响应式断点下模块是否完整、无错位溢出。

**视觉检查清单**

- [ ] 颜色是否均来自 token，禁止硬编码破坏双主题。
- [ ] 语义色是否仅用于 success / warning / danger 体系。
- [ ] 界面正文是否为 `--font-ui`，时间/代码是否为 `--font-mono`；若使用数据展示字体则为 `--font-data-display`；关键数字是否使用 `tabular-nums`。

### 约束 4：输出前必须检查（动效）

- [ ] 是否已对照 **§1.3 实现边界** 明确本交付物是「完整动效原型」还是「工程克制版（可省略卡片入场 / 悬停位移）」并在交付说明中写明。
- [ ] 若承诺完整动效：是否按 **§7 动画规范** 覆盖应存在的动画（如可选 `fadeInUp`、仪表盘 stroke 动画、进度条宽度动画、条形图分段动画、chart-shell 入场、刷新 `spin`、主题切换后的重新触发逻辑等）。
- [ ] 动效时长与缓动是否与 §7.1 / §7.5 表一致或等价，且不影响运维阅读（无过长、循环干扰主任务）。
- [ ] 主题切换或刷新后，需重新触发的动画是否已接入统一触发入口（参见 §7.4）。

---

## 附录 A · 修订历史（简表）

> 对外导出时以此表为主；若需审计细节，可对照 Git 提交记录或内部变更单。

| 版本     | 日期       | 摘要                                                                                                   |
| -------- | ---------- | ------------------------------------------------------------------------------------------------------ |
| **V2.5** | 2026-04-03 | 新增 §13 交付自检约束；修正 §10 子节编号（原误为 11.1–11.4）；补充导出用文档信息、目录与参考实现说明。 |
| V2.4     | 2026-04-02 | §2.6 浅色图表色板低饱和五色；§10.2 变量同步。                                                          |
| V2.3     | —          | §2.6 浅色静音化；§10.2 / §10.3 完整 `--chart-series-*`。                                               |
| V2.2     | —          | 卡片/面板/图表容器 `z-index` 与阴影不叠压；§5.2、§6.1 补充说明。                                       |
| V2.1     | —          | 结构整理与章节编号修复；条形图与蓝色色阶对齐。                                                         |
| V2.0     | —          | §7 横向条形图、图表容器动画；主题切换与刷新触发动画说明。                                              |
| V1.9     | —          | §2.2 五级蓝色 `--blue-1`…`--blue-5`；进度条主色统一。                                                  |
| V1.8     | —          | §5.6 页面 Header / 导航胶囊 / 时间 / 操作区规范。                                                      |
| V1.7     | —          | §12 移动端适配与测试清单。                                                                             |

---

\*文档版本：**V2.5\***（文件名中 `V1.5` 仅为历史命名）  
\*最后更新：**2026 年 4 月 3 日\***
